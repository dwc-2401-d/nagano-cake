<h2>商品詳細</h2>
<%= image_tag @item.item_image, size: "100x100" %>
<table>
  <tr>
    <th>商品名</th>
    <th><%= @item.name %></th>
  </tr>
  <tr>
    <th>商品説明</th>
    <th><%= @item.introduction %></th>
  </tr>
  <tr>
    <th>ジャンル</th>
    <th><%= @item.genre.name %></th>
  </tr>
  <tr>
    <th>税込価格（税抜価格）</th>
    <th><%= @item.price * 1.1 %> ( <%= @item.price %> )円</th>
  </tr>
  <tr>
    <th>販売ステータス</th>
  <% if @item.is_active == true %><!--itemのステータスが有効の時-->
    <th>販売中</th>
  <% else %><!--それ以外-->
    <th>販売停止中</th>
  <% end %>
  </tr>
</table>
<%= link_to "編集する", edit_admin_path(@item) %>
