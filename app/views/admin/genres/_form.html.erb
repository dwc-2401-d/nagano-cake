<%= form_with model: genre, url: admin_genres_path, method: :post, local: false do |f| %>
<div class="mt-4">
  <p>ジャンル名</p>
  <%= f.text_field :name, id: "genre_name" %>
  <%= f.submit "新規登録", class: "btn btn-success" %>
</div>
<% end %>
