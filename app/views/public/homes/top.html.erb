<div class="container p-4">
  <div class="row">
    <div class="col-md-3">
      <div class="row-mt-2">
        <%= render 'public/searches/genre_search', genres: @genres %>
      </div>
    </div>
    <div class="col-md-9">
      <div class="container-fluid">
        <div class="overlay">
        <%= image_tag "cake4.jpg", alt: "写真の説明", width: 780, height: 390 %>
          <div class="row align-items-start justify-content-start" style="height: 100%">
            <div class="col-mt-3" style="padding: 10px; margin-right: 10px; margin-top:12px;">
              <%= stylesheet_link_tag "public" %>
              <div class="text-overlay">
                <strong style="font-size: 25px;">ようこそ、ながのCAKEへ！</strong>
                <div>このサイトは、ケーキ販売のECサイトになります。<br>
                    会員ではない方も商品の閲覧はできますが、<br>
                    購入には会員登録が必要になります。
                </div>
              </div>
              </div>
            </div>
          </div>
      </div>

    <div class="col">
      <div class="text-center">
        <strong><h6 class="stylish-title">・・・New item・・・</h6></strong>
      </div>
        <div class="d-flex justify-content-center">
          <% @items.first(4).each do |item| %>
            <div class="mr-4">
              <% if item.is_active %>
                <%= link_to public_item_path(item.id) do %>
                <%= image_tag item.item_image, size: "200x130" %>
              <% end %>
                <div>
                  <b><%= item.name %></b>
                  <p>¥ <%= item.price.to_s(:delimited) %></p>
                </div>
              <% else %>
                <%= image_tag item.item_image, size: "100x100" %>
                <div>
                  <p><%= item.name %></p>
                  <p>販売停止中</p>
                </div>
              <% end %>
            </div>
          <% end %>
        </div>
        <div style="position: relative; top: -20px; text-align: right;">
        <%= link_to "全ての商品をみる▶", public_items_path %>
        </div>
    </div>
    </div>
  </div>
</div>
