<%= render 'public/searches/genre', genres: @genres %>

<div class="photo-container">
  <%= image_tag "image.jpg", alt: "写真" %>
  <%= stylesheet_link_tag "public" %>
  <div class="overlay">
    <p class="overlay-text">
      ようこそ、ながのCAKEへ！<br>
      このサイトは、ケーキ販売のECサイトになります。<br>
      会員ではない方も商品の閲覧はできますが、<br>
      購入には会員登録が必要になります。
    </p>
  </div>
</div>
 <div class="text-center">
  <h3>新着商品</h3>
  <div class="d-flex justify-content-center">
    <% @items.first(4).each do |item| %>
      <div class="mr-4">
        <% if item.is_active %>
          <%= link_to image_tag(item.item_image, size: "150x150"), public_item_path(item) %>
          <div>
            <p><%= link_to item.name, public_item_path(item) %></p>
            <p>¥ <%= item.price.to_s(:delimited) %></p>
          </div>
        <% else %>
          <%= image_tag item.item_image, size: "150x150" %>
          <div>
            <p><%= item.name %></p>
            <p>販売停止中</p>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
  <%= link_to "全ての商品をみる", public_items_path %>
</div>