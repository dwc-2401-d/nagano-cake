<div class="container p-4">
  <div class="row">
    <div class="col-md-8 offset-md-1">
      <div class="container-fluid">
        <%= image_tag "cake4.jpg", alt: "写真の説明", width: 800, height: 400 %>
          <div class="row align-items-start justify-content-start" style="height: 100%">
            <div class="col-mt-3" style="padding: 30px; margin-right: 10px; margin-top:12px;">
              <%= stylesheet_link_tag "public" %>
              <div class="overlay">
                <h4>ようこそ、ながのCAKEへ！</h4>
                <h6>このサイトは、ケーキ販売のECサイトになります。<br>
                    会員ではない方も商品の閲覧はできますが、<br>
                    購入には会員登録が必要になります。
                </h6>
              </div>
            </div>
          </div>
      </div>
    </div>

    <div class="col">
      <div class="text-center">
        <h6 class="stylish-title">・・・New item・・・</h6>
      </div>
        <div class="d-flex justify-content-center">
          <% @items.first(4).each do |item| %>
            <div class="mr-4">
              <% if item.is_active %>
                <%= link_to image_tag(item.item_image, size: "200x150"), public_item_path(item) %>
                <div>
                  <p><%= link_to item.name, public_item_path(item) %></p>
                  <p>¥ <%= item.price.to_s(:delimited) %></p>
                </div>
              <% else %>
                <%= image_tag item.item_image, size: "100x100" %>
                <div>
                  <p><%= item.name %></p>
                  <p>販売停止中</p>
                </div>
              <% end %>
            </div>
          <% end %>
        </div>
        <div style="position: relative; top: -20px; text-align: right;">
        <%= link_to "全ての商品をみる▶", public_items_path %>
        </div>
    </div>
  </div>
</div>
