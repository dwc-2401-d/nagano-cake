<div class="genre_search">
  <table class="table">
    <tr>
      <th>ジャンル検索</th>
    </tr>
      <% Genre.all.each do |genre| %>
        <tr>
          <td><%= link_to genre.name, "/genres/#{genre.id}/search/", method: :get %></td>
        </tr>
      <% end %>
  </table>
</div>