  <table class="table">
    <tr>
      <th>ジャンル検索</th>
  <%= form_with url: public_genre_search_path, method: :get do |f| %>
    </tr>
    <% Genre.all.each do |genre| %>
    <tr>
      <td>
        <%= link_to genre.name, "/genres/#{genre.id}/search/", method: :get %>
      </td>
    </tr>
    <% end %>
  <% end %>
</table>